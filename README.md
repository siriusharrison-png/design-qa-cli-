# Design QA CLI

检测代码是否正确使用设计系统 tokens，找出硬编码的样式值。

## 使用方法

```bash
# 进入项目目录
cd ~/Desktop/design-qa-cli

# 检测代码（默认使用 design-tokens.css）
node bin/cli.js check <目录>

# 示例
node bin/cli.js check ./src
node bin/cli.js check ../my-project/src

# 指定自定义 tokens 文件
node bin/cli.js check ./src --tokens ./custom-tokens.css
```

## 输出示例

```
🔍 Design QA 检测中...

   已加载 32 个颜色 tokens
   已加载 10 个间距规范值
   已加载 11 个字号规范值

📄 Button.tsx
   🎨 第8行  #1161fe → var(--brand-primary-1)
      颜色 #1161fe 应使用 var(--brand-primary-1)

📄 Card.css
   📏 第6行  15px → 16px
      间距 15px 不在规范内
   🔤 第13行  18px → var(--font-size-18)
      字号 18px 应使用 var(--font-size-18)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ 检测完成，发现 3 个问题：
   🎨 颜色问题: 1 个
   📏 间距问题: 1 个
   🔤 字号问题: 1 个
```

## 检测项

| 类型 | 说明 |
|------|------|
| 🎨 颜色 | 检测硬编码的 hex 颜色值，建议使用 CSS 变量 |
| 📏 间距 | 检测不在规范内的 px 值（padding、margin 等） |
| 🔤 字号 | 检测硬编码的字号，建议使用 CSS 变量 |

## 文件支持

支持扫描以下文件类型：
- `.js` / `.jsx` / `.ts` / `.tsx`
- `.css` / `.scss`
- `.vue` / `.svelte`

## Tokens 文件格式

支持两种格式：

### CSS 格式（推荐）

直接使用设计系统的 CSS 变量文件，变量名准确无误：

```css
:root {
  --brand-primary-1: #1161fe;
  --text-1: #181818;
  --space-16: 16px;
  --font-size-14: 14px;
}
```

### JSON 格式（兼容）

支持 Figma Design Tokens 插件导出的 JSON 格式。

## 下一步

- [ ] 发布到 npm（`npx design-qa check ./src`）
- [ ] 添加自动修复功能
- [ ] VS Code 插件版本
- [ ] HTML 报告输出
